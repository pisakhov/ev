<!-- model footer -->

<footer class="bg-violet-800 text-white fixed bottom-0 w-full z-[900] h-[60px]">
<div id="full-screen" class="-mt-[40px] flex items-center absolute w-64 text-center items-center gap-x-2 m-2 cursor-pointer text-sm md:text-md">
		<button id="fullScreenBtn" class="shadow-lg rounded-md p-1 md:p-2 w-1/2 bg-violet-900 hover:bg-violet-500 text-white active:bg-violet-300 active:text-white shadow-lg flex items-center justify-center gap-x-1"><i class="fas fa-expand after-icon"></i><p>Full Screen</p></button>
		<button id="toggleOffcanvas" class="shadow-lg md:flex hidden rounded-md p-1 md:p-2 w-1/2 bg-violet-900 hover:bg-violet-500 text-white active:bg-violet-300 active:text-white shadow-lg items-center justify-center gap-x-1"><i class="fas fa-question-circle after-icon"></i><p>Help</p></button>
	</div>
	<div id="helper" class="py-0 z-[110] relative flex items-end text-center gap-x-[2px] block md:hidden cursor-pointer">
		<button id="InputMobileBtn" class="pt-2 rounded-t-md w-full backdrop-blur-sm bg-violet-300 active:bg-white/50 hover:bg-violet-300 active:text-black shadow-lg flex items-center justify-center gap-x-1"><i class="fas fa-square-root-alt after-icon2"></i><p>Input</p></button>
		<button id="InputGraphBtn" class="pt-0 rounded-t-md w-full backdrop-blur-sm bg-white/80 active:bg-white/50 hover:bg-violet-300 active:text-black shadow-lg flex items-center justify-center gap-x-1"><i class="fas fa-chart-line after-icon2"></i><p>Graph</p></button>
		<button id="InputHelpBtn" class="pt-0 rounded-t-md w-full backdrop-blur-sm bg-white/80 active:bg-white/50 hover:bg-violet-300 active:text-black shadow-lg flex items-center justify-center gap-x-1"><i class="fas fa-question-circle after-icon2"></i><p>Help</p></button>
    </div>

    <div id="footer" class="container mx-auto px-6 py-3 flex justify-between items-center">
      <div class="flex items-center">
        <img src="https://econ.vision/assets/img/econdepartment.png" alt="Logo" class="h-4 md:h-6 w-auto" />
        <p class="ml-4 text-xs">&copy; <a class="text-white hover:underline hover:decoration-dotted hover:text-violet-300 transition-colors duration-300" href="mailto:rp3038@columbia.edu">Radi Pisakhov GS’23</a> v0.5</p>
      </div>
      <div class="text-right hidden md:block">
        <a href="/pages/about" class="text-white hover:text-violet-300 transition-colors duration-300 mr-4 text-xs">About Us</a>
        <a href="/pages/contact" class="text-white hover:text-violet-300 transition-colors duration-300 text-xs">Contact</a>
      </div>
    </div>
</footer>
<!-- end model footer-->


      <script>

$(document).ready(function() {
  var nav = $('nav');
  var navLinks = $('.zebra-1');
  var navLogo = $('.zebra-2');
  var navHamIcon = $('.zebra-3');

  function updateNavColors() {
    var scroll = $(window).scrollTop() + nav.outerHeight();
    var sections = $('.dark-section, .light-section');
    var currentSection = null;

    sections.each(function() {
      if (scroll >= $(this).offset().top) {
        currentSection = $(this);
      } else {
        return false; // Break out of the loop when the current section is found
      }
    });

    if (currentSection && currentSection.hasClass('light-section')) {
      navLinks.removeClass('text-gray-100').addClass('text-gray-600');
      navLogo.removeClass('text-violet-300').addClass('text-violet-600');
      navHamIcon.removeClass('text-violet-100').addClass('text-violet-600');
    } else {
      navLinks.removeClass('text-gray-600').addClass('text-gray-100');
      navLogo.removeClass('text-violet-600').addClass('text-violet-300');
      navHamIcon.removeClass('text-violet-600').addClass('text-violet-100');
    }
  }

  $(window).on('scroll', updateNavColors);
  updateNavColors(); // Call it once on page load to set the initial colors
});




         $(document).ready(function() {
           function animateNumbers(element, targetValue) {
         	let currentValue = 0;
         	let interval = setInterval(function() {
         	  if (currentValue >= targetValue) {
         		clearInterval(interval);
         		return;
         	  }
         	  currentValue += Math.ceil(targetValue / 100);
         	  element.text(currentValue.toLocaleString());
         	}, 20);
           }
          
           const $numberElements = $("[data-count]");
           $numberElements.waypoint(
         	function() {
         	  const $element = $(this.element);
         	  const targetValue = parseInt($element.attr("data-count"), 10);
         	  animateNumbers($element, targetValue);
         	  this.destroy();
         	},
         	{
         	  offset: "90%",
         	}
           );
         });
          
      </script>
      <script>
         $(document).ready(function () {
           const courseLinks = $('.course-link');
           const subcourseLists = $('.subcourse-list');
          
           // Set the first course to active and apply the text-violet-900 class
           //courseLinks.eq(0).removeClass('text-white').addClass('active text-violet-900');
          
           courseLinks.on('click', function (e) {
         	e.preventDefault();
          
         	// Remove active class from all course links
         	courseLinks.removeClass('active text-violet-900').addClass('text-white');
          
         	// Add active class and text-violet-900 class to the clicked course link
         	$(this).addClass('active text-violet-900').removeClass('text-white');
          
         	// Hide all subcourse lists
         	subcourseLists.addClass('hidden');
          
         	// Show the subcourse list for the clicked course
         	const courseId = $(this).data('course');
         	const subcourseList = $(`.subcourse-list[data-course="${courseId}"]`);
         	subcourseList.removeClass('hidden');
           });
         });
          
      </script>
      <script>
         $(document).ready(function () {
           // Toggle courses accordion on click
           $(".courses-btn").on("click", function (e) {
         	e.stopPropagation();
         	$(this).siblings(".courses-accordion").slideToggle(300);
           });
          
           // Toggle subcourses on click
           $(".course > a").on("click", function (e) {
         	e.stopPropagation();
         	$(this).siblings(".subcourse").slideToggle(300);
           });
          
           // Close accordion when clicking outside
           $(document).on("click", function () {
         	$(".courses-accordion, .subcourse").slideUp(300);
           });
         });
          
      </script>
      <script src='https://econ.vision/js/typewriter.js'></script>
      <script type='text/javascript'>
         $(document).ready(function(){
           $(window).scroll(function(){
             var viewportTop = $(window).scrollTop();
             var viewportBottom = viewportTop + $(window).height() - 100;
             var featuresSectionTop = $('.scroll-this').offset().top;
             var featuresSectionHeight = $('.scroll-this').height();
             var featuresSectionBottom = featuresSectionTop + featuresSectionHeight;
         
             if (featuresSectionBottom > viewportTop && featuresSectionTop < viewportBottom) {
         		$('.scroll-this').addClass('scale-105');
         		$('.scroll-this div:first-child').removeClass('md:h-auto').addClass('md:h-auto');
             } else {
         		$('.scroll-this').removeClass('scale-105');
         	  $('.scroll-this div:first-child').removeClass('md:h-auto');
             }
           });
         });
                
         
         		$(".nav-button-mobile").click(function() {
         			$(".nav-menu-mobile").toggleClass("hidden");
         		});
         		const typewriter = new Typewriter('#mainlogo', {
         			autoStart: true
         		});
         		typewriter.typeString('Envision economic models.').pauseFor(1500).deleteChars(24).typeString('con').pauseFor(500).typeString('<strong>.</strong>').pauseFor(100).typeString('vision').pauseFor(2500).start();
         	
      </script>
      <script defer src="https://econ.vision/js/model.js"></script>